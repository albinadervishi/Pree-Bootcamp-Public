import React from "react";

const navStyle = {
    width: '210px',
    height: '350px',
    backgroundColor: "blue" ,
    display: 'inline-block',
    margin: '10px 15px',
    // position : 'absolute',
    // top: '180px',
    // left: '0px'
};

const Nav = (props) => {

    return (
        <div style={navStyle}>
        </div>
    );
}
    
export default Nav;